[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "REWIND"
version = "0.1.0"
description = "Wind LCA modeling tools"
authors = [{ name = "Dominik Huber" }]

[tool.setuptools]
packages = ["REWIND"]